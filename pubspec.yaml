name: qrcode
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_statusbarcolor_ns: ^0.5.0
  qr_code_scanner: ^1.0.1

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  qr_flutter: ^4.1.0

  flutter_lints: ^3.0.0

flutter:

  uses-material-design: true

 
  assets:
    - lib/assets/img/happyguy.png
    - lib/assets/img/qrcode.png
  